version: "3.9"

services:
  postgres:
    image: postgis/postgis:15-3.3
    container_name: postgres
    restart: always

    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: sFdP4QAgP&
      POSTGRES_DB: rotas_seguras

    ports:
      - "5432:5432"  # ⚠️ NÃO usa 5432 (evita conflito)

    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:

